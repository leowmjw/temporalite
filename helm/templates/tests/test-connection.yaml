apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "temporalite.fullname" . }}-test-connection"
  labels:
    {{- include "temporalite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "temporalite.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
